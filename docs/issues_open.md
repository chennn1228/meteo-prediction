# 未解决问题

协议 `2.1.0-provisional-15var-archive-aligned`；2026-09-24。这里记录**仍阻断正式重跑**或需要
证据关闭的事项。旧问题历史保存在 `archive/docs/`，不代表已获新协议认可。

| ID | 优先级 | 仍需解决 | 关闭证据 |
|---|---|---|---|
| P0-DATA-01 | P0 | archive-aligned 15 变量的当前请求缓存只完成南京 93/1,860 个月文件；旧文件虽已离线核验字节、日志和坐标，但缺完整原请求参数，不能补造侧车 | 取得可验证的旧请求 provenance，或在允许的节奏下按当前合同补齐 19 站；不准伪造侧车 |
| P0-TRUTH-01 | P0 | 固定 20 站当前协议的独立小时 Himawari 真值收据未完成；旧缓存审计发现 1,317 个白天真值缺测站-小时 | 20 站逐小时覆盖/缺失与可评分集合审计收据；不把缺测当有效真值 |
| P0-CPU-01 | P0 | 除 raw_gfs 外九个 CPU 模型均仍 pending_validation；南京真实 mini-E2E、五外折和最终测试只证明单站诊断链 | 各模型验证/准入、20 站共同正式特征与真实执行收据通过 |
| P0-MODEL-01 | P0 | 深度实现尚未逐个通过标准架构/张量/早停/损失审计；AutoCorrelation lag 聚合需专项核验 | 逐模型机制审计与针对性测试，未过者降级/改名或替换 |
| P0-PIPE-01 | P0 | 新 `s01`–`s15` 模块与旧训练代码并存；南京诊断链已贯通，但 20 站正式编排/准入仍缺 | 统一预测合同贯通正式 20 站、无 legacy import、完整集成/E2E 和强校验通过 |
| P1-FEAT-01 | P1 | 折内特征/插补无泄漏需以真实数据和旧入口迁移再审 | issue-time 可得性、折内拟合、云量插补边界与身份禁入检查 |
| P1-INTERP-01 | P1 | 单站开发期 SHAP/残差密度、15 内折组消融与组置换已出；Base+cloud/kt 对照、滚动稳定性与区间证据尚未完整完成 | 所有选择只在开发期内折；SHAP 仅解释，不反向选特征 |
| P1-CAL-01 | P1 | 南京最终测试的因果校准已跑通；20 站正式概率评价与真实时间块区间仍缺 | 20 站后期校准集、issue-time 无泄漏验证、分组覆盖与不确定性审计 |
| P1-FIG-01 | P1 | 南京 16 张真实诊断图已生成并复核；省域/正式论文图仍缺合法输入 | 正式结果输入后重制并审查，不能给南京图改身份 |
| P1-GFS-ZERO | P1 | raw GHI 在太阳高度 >15°仍有极少零值，其中 1 行同时 Himawari GHI >300 W/m²；需逐例核查产品/时间语义 | 逐例来源确认；不得用 clean 后零值率代替 raw 证据 |
| P1-PINN-01 | P1 | PINN 非负/晴空约束的物理单位和合理性未验证 | W/m² 空间约束、enhancement/tolerance 与约束消融；此前保持实验性 |

这些阻断不是测试失败的掩饰。以 `scripts/01_validate/run.py --mode cpu_ready`
和 `reports/protocol_migration_audit.md` 的本轮快照为准；不得生成或标记
`official_result_set`。

已关闭的旧项：`P0-SPLIT-01`——用户明确批准把内层评分改为 14 天，原定窗口不缩，首外折三内折真实样本均非空，五外折调参已跑通。服务点全集与空间 Level/密度研究按用户要求暂缓，不列为当前固定 20 站 CPU 的即时门槛；恢复空间任务时仍须重新审计，绝非视为已完成。

已关闭：`P1-CLOUD-18`——Open-Meteo 官方确认 Previous Runs 历史档案不保存 low/mid/high cloud；正式历史主线使用 total cloud，删除不是为让训练通过。
